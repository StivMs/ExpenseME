<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="da345af1.ai2530.mah.se.p1_stivan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OverViewActivity"
            android:label="Översikt" />
        <activity
            android:name=".OutcomeActivity"
            android:label="Utgifter"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".AddOutcomeActivity"
            android:label="Lägg till en utgift" />
        <activity
            android:name=".IncomeActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".AddIncomeActivity" />
        <activity android:name=".IncomeMoreInfoActivity" />
        <activity android:name=".OutcomeMoreInfoActivity" />
        <activity android:name=".Deficit_SurplusActivity"></activity>
    </application>

</manifest>